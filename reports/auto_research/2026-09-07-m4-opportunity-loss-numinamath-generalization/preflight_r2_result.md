# NuminaMath paired R2 no-training preflight result

Status: `GREEN_NO_TRAINING_PREFLIGHT`.

Parent pipeline `66799474`, downstream pipeline `66799605`, and all component
jobs succeeded. Slurm job `5994386` and rank 0 exited zero. The terminal
logs-after artifact has SHA-256
`f2de4a664c6ed15048fc13d009edc9dc65da897bdc48ab018c8211501818f58e`,
contains 33 members, and passes ZIP integrity.

The pinned environment completed 121 selected tests, configuration resolution,
Ruff, compilation, the three-shard dataset load, and repaired dataset mapping.
The lock records 680,786 retained rows and 680,786 unique nonblank problems.
All 50 locked files independently match the deterministic source archive
byte-for-byte. Lock SHA-256 is
`805d06573a6927c4ff773455b9e30d0c7651399b1e2070acb4d2642591db8915`.

This was strictly a no-training preflight. It started no training,
qualification, or scientific acquisition; produced no causal estimate; and
performed no retry or extension. Therefore it validates the repaired package
and candidate contract but provides no evidence about the M4 effect.

The next scientific gate is to freeze matched neutral 32-step qualification
packages for both prospective model cells. Qualification observations must
remain excluded from every causal estimator, and qualification execution needs
separate explicit training authority.
