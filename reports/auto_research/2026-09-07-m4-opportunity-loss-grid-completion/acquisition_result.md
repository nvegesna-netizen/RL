# Qwen3-0.6B/GSM8K M4 grid-completion result

Status: `MATERIAL`.

The prospectively registered primary adjusted estimator supports a material M4
opportunity-loss effect in the tested Qwen3-0.6B/GSM8K setting. The estimate is
`0.2323419953`; the registered 95% HAC/bootstrap outer envelope is
`[0.2107693490, 0.2532757032]`, wholly above the `0.20` threshold. The material
p-value is `0.0015999200` against the registered `0.05` criterion.

The acquisition completed all 558 steps and contributed 9,573 primary
assignments, above the 7,395 minimum. Control had 4,766 assignments and d5 had
4,807. Both arms had zero missing terminal dispositions, so the lower and upper
sharp endpoints coincide.

The supporting unadjusted estimate is `0.2468378656`, with outer envelope
`[0.1838269038, 0.3068576633]` and conclusion `INCONCLUSIVE`. That continuity
estimator cannot override the prospectively registered adjusted primary result.

The mechanism replicated: d5 direct-chain rate was `0.3998335760` versus zero
under control, and the d5-minus-control version-advance contrast was
`0.6461392547`. Corrected observer duty was `0.0032864855`, below the `0.01`
portability ceiling, so observer-duty support is `SUPPORTED`.

The red pipeline status reflects a post-acquisition manifest defect, not an
incomplete acquisition. The manifest selected the generic analyzer, which
rejected the grid protocol identity after all data had been written. The frozen
workload-transport analyzer explicitly registered for this protocol recovered
the exact 20,000-draw result from the immutable terminal artifacts. No new data,
protocol change, retry, or extension entered the recovery.

The terminal artifact SHA-256 is
`6c2ebeb2b2bf67df16be815f372ea87cde8eeff4d23e63291d8a98ac94659075`.
The recovered analysis result SHA-256 is
`26d1650143c502e5790f194ab7c703051820e47993c8fcd9206bbec2bd7f10c2`.

This conclusion is limited to the tested model, GSM8K workload, instrument,
runtime, and environment. The subsequently completed secondary common-window
four-cell grid synthesis detected an interaction, but cannot retroactively
alter this primary cell conclusion.
